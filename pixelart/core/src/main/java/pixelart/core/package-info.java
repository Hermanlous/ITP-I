/**
 * The Grid and pixel classes handles the making
 * and management of a grid of pixels.
 * GridStateHandler saves the grid state to a JSON file,
 * and loads the grid state from a JSON file.
 * The grid consists of Canvas elements, where each canvas
 * represents a pixel. Each Canvas is black or white.
 */
package pixelart.core;
