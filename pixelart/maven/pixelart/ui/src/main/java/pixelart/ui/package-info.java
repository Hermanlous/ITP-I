/**
 * The App class is the starter for the JavaFX application Pixelart
 * It sets up the FXML layout, sets up the scene, and displays the pixelart.
 * The application is for making pixel art.
 * AppController is the main controller class for the JavaFX application
 * pixelart. It controls the interaction between the user and the
 * 100x100 grid of canvases. The users can draw or erase on the
 * different pixels  using left and right mouse clicks.
 * Which pixels that are coloured in are saved in a text file.
 */

package pixelart.ui;
